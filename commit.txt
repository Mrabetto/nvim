Added keybinds, folding options, auto cmp options, and C lsp
# Keybinds Added :
    - <leader>q to quit (requires confirmation with unsaved changes)
    - <A-hjkl> (Or Alt + hjkl) to move focus between visible windows in the same tab/buffer?
# Folding options (finally):
    - stolen from some guy on github, credit in the config file
    - specific folding option for python(indent instead of brackets and stuff)
    - highlight on yank, yoinked from the same guy probably
# Auto cmp & lsp :
    - changed the default confirmation to enter, I think I am regretting it, but I dont use autocmp enought maybe, will probably change back
    - set the default documentation option to on, I love seeing either the syntax of something and its arguments
    - FINALLY added the c lsp, and I was wondering why I couldn't any of the errors until I tried to compile
    - I also added some textobject-related stuff with treesitter, in order to better select certain code object, such as function or arguments, which did not work with mini.ai especially around indent based scoping in python (althought I cant seem to find it in the diff output from git, so maybe already document in a previous commit)
